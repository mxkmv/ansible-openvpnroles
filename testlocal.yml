---
 - name: check localscript for test
   hosts: localhost
   become: yes
   connection: local  
   tasks:
   
   - name: run_local_script 
     shell: "/home/xxx/localtest/testone.sh"
     register: "output"
     
   - debug: var=output.stout_lines
 
...
